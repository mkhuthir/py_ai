# NLTK Setup

First you need to install the nltk library with:

```bash
$ sudo pip install -U nltk
```

Then you need to download the nltk corpus by running:

```python  
>>> import nltk  
>>> nltk.download()
```


